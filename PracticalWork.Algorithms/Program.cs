using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;

namespace PracticalWork.Algorithms
{
    class Program
    {
        
        static void Main(string[] args)
        {
            //var searcher = new Searcher<int>();

            //var unsortedArray = new[] { 1, 2, -10, 22, 3 };
            //var sortedArray = new[] { -10, 1, 2, 3, 22 };

            //Console.WriteLine(searcher.LinearSearch(unsortedArray, 1));
            //Console.WriteLine(searcher.BinarySearch(sortedArray, 1));

            //var list = new LinkedList<int>();
            //list.EmplaceBack(1);
            //list.EmplaceBack(2);
            //list.EmplaceFront(100);

            //foreach (var el in list)
            //{
            //    Console.WriteLine(el);
            //}

            //var tree = new BinaryTree<int>();
            //tree.AddToTree(8);
            //tree.AddToTree(3);
            //tree.AddToTree(5);
            //tree.AddToTree(10);

            //Console.WriteLine(tree.Find(-1));
            //var elements = tree.InfixTraverse().ToList();
            //foreach (var el in elements)
            //{
            //    Console.WriteLine(el);
            //}

            //var p = new PermutationsGenerator<string>(new List<string>() {"Masha", "Dasha", "Nastya"});
            //var res = p.GetPermutations().ToList();

            //foreach (var perm in res)
            //{
            //    foreach (var el in perm)
            //    {
            //        Console.Write("{0} ", el);
            //    }

            //    Console.WriteLine();
            //}
        }
    }
}
